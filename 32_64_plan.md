# 32 位 / 64 位开发路线规划

> 决策时间：2026-07-02  
> 适用范围：单人开发、教学内核、当前已完成 HAL 6 级 + 网络栈 + 多任务 + 驱动/FS/GUI 基础结构

---

## ✅ 最终决策：先把 32 位做成熟，再扩展 64 位

```
推荐路线：
  ┌────────────────────────┐   (3~4 周)   ┌─────────────────────────┐   (2 周)   ┌──────────────┐
  │ 当前：HAL6 + 网络 + 多任务 │ ──────────→ │ 32位成熟：VFS/shell + oops │ ────────→ │ 64位移植版 │
  └────────────────────────┘              │ + 用户态 ring3 + syscall │            └──────────────┘
                                           └─────────────────────────┘
```

---

## ❌ 为什么不推荐「先扩展 64 位，再继续优化内核」

### 根本原因：同时掉进两个无底洞，互相干扰，极度容易放弃

```
典型节奏（基于 LightOS 32→64 改造踩坑经验）：
  第1天：改 Makefile -m64 → 120 个 elf32/64 不兼容错
  第3天：写长模式 stub + 4 级页表 → QEMU 三重故障黑屏
  第7天：串口能 printf 了 → AMD 网卡 DMA 64 位地址崩
  第9天：网卡修了 → 多任务 ctx switch 8→16 寄存器全重写
  第14天：调度能跑了 → 回头看原本要写的 VFS，思路全忘
→ 结果：两边都半残，也分不清 bug 是 64 位切换写错还是功能逻辑错
```

三重故障黑屏（PML4 写错 / GDT L 位错 / SSE 栈未 16 字节对齐）是最大杀手：
**无任何输出、无 EIP、只能在 Bochs 里单步对照 Intel 手册翻 → 挫败感极强。**

---

## ✔️ 四个维度对比：为什么先 32 位成熟再 64 位收益最大

| 维度 | 先 32→再 64 ✅ | 先 64→再功能 ❌ |
|---|---|---|
| 📈 **学习曲线** | 32 位资料最多：OSDev Wiki / 原 C++ 教程 / B 站视频全是 32 位；每个功能（VFS/shell/syscall）快速反馈 | 长模式切换 / 4 级页表 / 中断栈 / SSE 对齐 都是独立难点，和内核功能无关；**双倍学习量** |
| 🐞 **调试难度** | 三重故障概率低；panic 可打 EIP/EBP；oops 一做，任何 bug 定到行号 | 长模式/PML4/GDT L 位/16B 对齐/movdqa/syscall 破坏 rcx·r11 —— **任何一步错都是黑屏重启** |
| 🎮 **成就感（最重要）** | 每 1~2 周看到实东西：这周 ls，下周 cat，再下周 ring3 hello → 动力源源不断 | 连续 2 周只看到"有没有从黑屏变串口打 K"，极易放弃 |
| ♻️ **代码复用率** | HAL 6 级隔离优势：net/FS/gui/调度/驱动逻辑 **~100% 复用**；移植 64 位只用改 arch/x86 + hal/io + hal/spinlock + Makefile/ld（<20% 代码） | 20% 底层移植 和 80% 功能开发 **耦合并行、互相污染**，复用率反而更低 |

---

## 🎯 最大隐藏王牌：你们的 HAL 层已经做完了

普通项目（LightOS / 玩具内核）32→64 移植要改**驱动里成千上万行**直接写 port / cli 的代码。

本项目？：
- 所有驱动/网络/FS 只调 `jlos_io8_write()` / `jlos_spin_lock_irqsave()` 这种 HAL API
- 移植 64 位只需：
  1. `hal/io.c` 加一张 `jlos_hal_x86_64_fast_io_ops` 函数表
  2. `hal/spinlock.h` 把 `xchgl`（4字节）换成 `xchgq`（8字节）
  3. `arch/x86/` 加 loader stub（32→长模式切换）、64 位 interruptstubs、64 位 ctx switch
  4. `Makefile` 支持 `ARCH=x86` / `ARCH=x86_64` 两套工具链 flag
- **工作量为普通项目的 1/5**

这个架构优势必须先**在 32 位上把功能堆厚**，再**靠 HAL 层快速切 64 位**，反过来就是浪费。

---

## 🚦 什么时候可以启动 64 位移植？三条判定标准（满足任意 2 条即可）

| # | 标准 | 目的 |
|---|---|---|
| 1 | ✅ **功能**：最小 VFS + shell，能 `ls/cat/cd` 任意目录文件 | 证明 HAL 隔离的上层链路（FS/驱动/调度/IO）100% 跑通，移植 64 位时这些完全不用动 |
| 2 | ✅ **调试**：内核 panic 时能打栈回溯 `EIP → func+offset`，不靠 Bochs 单步 | 移植 64 位时哪怕黑屏，至少能看到 panic 到哪条指令，不会放弃 |
| 3 | ✅ **用户态**：至少 1 个 ring3 进程通过 `int 0x80` 调 `sys_write` 打串 + `sys_exit` 退出 | 证明已掌握 ring 切换 / 中断栈 / syscall 三件事，64 位 syscall/sysret/iretq 都是同概念的 64 位写法 |

---

## ⏱️ 两种路线的时间预估对比

| 路线 | 3 周后 | 6 周后 |
|---|---|---|
| ✅ **32→再64** | VFS + shell 可用，能 `ls` 根目录 | 32 位已成熟 + 64 位移植完成 = 功能完备的 64 位内核 |
| ❌ **先64→再功能** | 勉强串口打 `K` 的 64 位空壳，无 VFS 无用户态 | 64 位勉强能调度 2 个内核线程，VFS/shell 全没做，代码还埋着 32→64 边界暗坑 |

---

## 📌 当前阶段最重要的事：消化 & 完全掌握核心代码

> 「既然我一个人在做，那么就需要完全掌握它们」—— 这个判断非常正确。  
> HAL、多任务、网络栈是未来一切功能（VFS、shell、用户态、64 位移植）的地基，现在不啃透，后面每一步都会反复回来翻。

建议按以下顺序逐一掌握，每个模块做到：**能不看源码重写出关键数据结构和入口函数调用链**。

### 掌握顺序（从地基到上层）

```
第 1 步（1~2 天）：arch/x86 最底层
  ├── gdt.c / gdt_asm.s     →  代码段(0x08) 数据段(0x10) flat 布局，为什么 limit=0xFFFFFFFF
  ├── idt.c / interrupts_asm.s
  │     └── 中断 stub 压栈顺序 ↔ jlos_cpu_state_t 成员顺序（100% 对齐！你们踩过 EIP=0x00000003 坑）
  ├── interruptstubs.s
  │     └── 为什么 error code / no error code 要分支，为什么用 movl $intnum, %eax（4字节）不能用 pushb
  └── pic.c / pit.c        →  PIC 默认 mask 0xFA（只开 IRQ0+IRQ2），PIT 100Hz 怎么算 11932 分频

第 2 步（2~3 天）：HAL 6 级（你们项目的核心壁垒）
  ├── hal.h + hal.c         →  jlos_hal_io_ops_t 运行时多态表，jlos_hal_info_t 只读硬件信息聚合
  ├── io.h / io.c           →  jlos_io8/16/32 读写分别走哪张 ops 表
  ├── spinlock.h            →  recursion_depth + irq_state 嵌入 struct 的原因（原来散在 kernel.c 的 s_printf_depth）
  ├── device.h              →  统一设备模型：资源注册 → 冲突检测 → 回滚的流程
  ├── pci.c / serial.c / ata.c / dma.c
  │     └──  5 类 HAL 硬件包装各自暴露了什么 API（供驱动调用）
  └── diag.h / diag.c       →  HAL_CONFIG_TRACE_IO=1 时 512 项环形缓存怎么写入 / dump

第 3 步（2 天）：kernel 核心
  ├── memory_manager.c      →  双堆切换：为什么 PCI 枚举时要临时切到低地址堆（堆分配驱动结构）
  ├── multitask.c + multitask.h
  │     ├──  cpustate 嵌入 jlos_task_t 结构体（而不是放 task 栈）的原因（你们踩过 interrupt 覆盖）
  │     └──  RR 调度怎么跳过 JLOS_TASK_TERMINATED 任务
  ├── kernel.c 三阶段启动
  │     ├──  Stage1: HAL arch init + PCI 枚举绑驱动 + AMD 网卡结构堆分配
  │     ├──  Stage2: AMD 网卡 STOP→CSR4→CSR1/2→INIT→STRT(0x42) 严格顺序
  │     └──  Stage3: PIT 100Hz + sti 开中断 + network_init（顺序不能反，否则 ARP 死锁）
  └── printf 线程安全：s_printf_lock 在 kernel.c 的使用

第 4 步（2 天）：网络栈（你们最复杂的分层）
  ├── etherframe_provider.c →  handler 表（按 ethertype 分发给 ARP / IPv4）
  ├── arp.c                 →  ARP 广播 → 5M 次 timeout 轮询（替代 hlt 防死锁）
  ├── ipv4.c / icmp.c       →  checksum 计算
  ├── udp.c / tcp.c         →  socket 数组（JLOS_NET_MAX_SLOTS 不是硬编码 65535）
  └── drivers/amd_am79c973.c
        ├── INIT 块必须 32 字节对齐（物理地址低 5 位 = 0）
        ├── CSR3/CSR5 绝对不要手动写（硬件从 INIT 块读）
        ├── last_processed_iter vs processed_count 推进 RX 环的区别（防 MISS ERROR）
        └── 发描述符要带 ENP(0x00800000)，BS 字段在 bits 19-16

第 5 步（最后）：FS + GUI + 驱动管理（结构已搭好，细节等写功能时再啃）
```

掌握完以上 4 步（共 7~10 天），再去写 VFS/shell / oops / 用户态，**你会发现"这个坑我见过"，不再是边写边踩新坑**，速度会快很多。

---

## 💡 一句话总结

> **先堆高度（功能+调试能力+用户态），再换底座（HAL 隔离让 64 位移植省力 80%）。**
>
> 如果反过来（同时换底座+盖楼），楼会歪两次，最后你会累到不想盖了。
